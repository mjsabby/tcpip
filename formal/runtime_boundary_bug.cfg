\* TLC configuration for runtime_boundary.tla — the PRE-FIX protocol
\* (record `emitted = desired` even when the push was shed).
\*
\* This run is EXPECTED TO FAIL: TLC finds the stall counterexample
\* (desired = emitted, queue empty, armed # desired). check.sh runs it
\* and asserts the violation IS found — a vanishing counterexample means
\* the model no longer captures the bug class.
SPECIFICATION Spec
CONSTANTS
    MaxChanges = 4
    QCap = 1
    RecordOnShed = TRUE
INVARIANT TypeOK
INVARIANT QuiescentFaithful
PROPERTY Converges
